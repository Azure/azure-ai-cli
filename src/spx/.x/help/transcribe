USAGE: spx transcribe [...]

PREVIEW NOTE

  The Azure Conversational Transcription Service is currently in
  PREVIEW. As a result, CTS does not support all regions or languages.

    Supported regions: centralus, eastasia
    Supported languages: en-US, zh-CN

OPTIONs

  CONNECTION                      (see: spx help transcribe connection)
    --key KEY                     (see: spx help transcribe key)
    --region REGION               (see: spx help transcribe region)
    --endpoint URI                (see: spx help transcribe endpoint)
    --token VALUE                 (see: spx help transcribe token)

  CONVERSATION
    --conversation id ID          (see: spx help transcribe conversation id)

  LANGUAGE                        (see: spx help transcribe language)
    --source LANGUAGE             (see: spx help transcribe source)

  INPUT                           (see: spx help transcribe input)
    --file FILENAME               (see: spx help transcribe file)
    --files @FILELIST.txt         (see: spx help transcribe files)

  OUTPUT                          (see: spx help transcribe output)
    --output all [<item>]         (see: spx help transcribe output all)
    --output each [<item>]        (see: spx help transcribe output each)

  ADVANCED                        (see: spx help transcribe advanced)
    --connect                     (see: spx help transcribe connect)
    --log FILENAME                (see: spx help transcribe log)
    --proxy HOSTNAME              (see: spx help transcribe proxy)
    --phrases @PHRASES.txt        (see: spx help transcribe phrases)
    --foreach in @ITEMS.txt       (see: spx help transcribe foreach)
    --save FILENAME               (see: spx help transcribe save)
    --zip ZIPFILE                 (see: spx help transcribe zip)

EXAMPLE                           (see: spx help transcribe examples)

  spx transcribe --file 8-channel-audio.wav.wav

SEE ALSO

  spx help setup
