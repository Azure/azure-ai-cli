CHAT

  The `ai chat` command initiates interactive or non-interactive conversations
  with an AI language model (e.g. OpenAI's GPT4).

USAGE: ai chat [...]

  CONNECTION                            (see: ai help connection)
    --deployment DEPLOYMENT             (see: ai help chat deployment)
    --endpoint ENDPOINT                 (see: ai help chat endpoint)
    --key KEY                           (see: ai help chat key)

  INPUT                                 (see: ai help chat input)
    --interactive                       (see: ai help chat interactive)
    --system PROMPT                     (see: ai help chat system prompt)
    --file FILE                         (see: ai help chat history file)`#e_;*`
    --files PATTERN                     (see: ai help chat history files)`#e_;*`
    --files @FILELIST.txt               (see: ai help chat history files)`#e_;*`
    --json FILE                         (see: ai help chat history json)`#e_;*`
    --jsonl FILE                        (see: ai help chat history jsonl)`#e_;*`
    --user MESSAGE                      (see: ai help chat user message)

  `#e_;* - NOT IMPLEMENTED YET`

  CHAT WITH DATA                        (see: ai help chat with data)
    --data DATA                         (see: ai help chat data)`#e_;*`
    --data-file FILE                    (see: ai help chat data file)`#e_;*`
    --data-files PATTERN                (see: ai help chat data files)`#e_;*`
    --data-files @FILELIST.txt          (see: ai help chat data files)`#e_;*`
    --embeddings-deployment DEPLOYMENT  (see: ai help chat embeddings deployment)`#e_;*`
    --search-index INDEX                (see: ai help search index)
    --search-endpoint ENDPOINT          (see: ai help search endpoint)
    --search-api-key KEY                (see: ai help search key)

  OPTIONS                               (see: ai help chat options)
    --temperature TEMPERATURE           (see: ai help chat options temperature)
    --max-tokens MAX_TOKENS             (see: ai help chat options max-tokens)
    --top-p TOP_P                       (see: ai help chat options top-p)
    --n N                               (see: ai help chat options n)

EXAMPLES

  `EXAMPLE 1`: Chat interactively using a specific system prompt

    ai chat --interactive --system @prompt.txt

  `EXAMPLE 2`: Chat (non-interactively) using a specific system prompt and user input

    ai chat --system @prompt.txt --user "What can you do?"

  `EXAMPLE 3`: Chat using chat message history stored in a file

    ai chat --file chat-history.txt --user "Tell me a joke"

  `EXAMPLE 4`: Chat w/ data using in-memory vector index (real-time embeddings)

    ai config @ada --set text-embedding-ada-002
    ai service openai deployment create --name @ada --model @ada --wait
    ai chat --data-files *.txt --embeddings-deployment @ada --interactive

  `EXAMPLE 5`: Chat w/ data using Azure Search vector index (pre-computed embeddings)

    ai config @myindex --set MyIndex
    ai search index create --name @myindex --files *.txt --interactive
    ai chat --search-index @myindex --interactive

SEE ALSO

  ai help chat setup
  ai help chat advanced
  ai help chat examples
  ai help find topics chat