INTENT ZIP

@include.the.intent.command

@include.the.zip.option

  NOTE: `--zip` does not package local input files (e.g. audio, models, etc.)

USAGE: spx intent [...] --zip FILENAME

EXAMPLES

  spx intent --files *.wav --zip test1.zip
  spx intent --files *.wav --log {id}.log --zip test2.zip
  spx intent --foreach file;transcript in @items.txt --zip test3.zip

  spx intent --files @URLs.txt --output zip output.zip --zip test4.zip

SEE ALSO

  spx help intent
  spx help intent files
  spx help intent foreach
