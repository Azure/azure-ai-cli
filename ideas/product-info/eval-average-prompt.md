Find on disk evaluations in files with this pattern: `{pattern}`

Calculate the average score across all evaluations.

Only output a floating point number, the average score, with up to two decimal places.
